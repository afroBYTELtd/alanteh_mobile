name: asm_app_config
description: Typed market, runtime, and capability configuration for ASM apps.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter
